#
# © 2026-present https://github.com/cengiz-pz
#

name: Draft Release on Milestone Close

on:
  milestone:
    types: [closed]

permissions:
  contents: write   # required to create releases and upload assets

jobs:
  draft-release:
    name: Build archives & create draft release
    if: ${{ !github.event.repository.is_template }}
    runs-on: macos-latest

    env:
      TAG_NAME: v${{ github.event.milestone.title }}

    steps:
      # ── Checkout ────────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # ── Node ────────────────────────────────────────────────────────────────
      - name: Install Node
        uses: ./.github/actions/install-node

      # ── Java ────────────────────────────────────────────────────────────────
      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      # ── Gradle ──────────────────────────────────────────────────────────────
      - name: Grant execute permission for Gradle wrapper
        run: chmod +x common/gradlew

      # ── Prepare for iOS build ───────────────────────────────────────────────
      - name: Install SCons
        if: env.ACT != 'true'
        run: pip install scons

      # ── Prepare for Android build ───────────────────────────────────────────
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # ── Create archives ─────────────────────────────────────────────────────
      - name: Run createArchives
        working-directory: common
        run: ./gradlew createArchives --no-daemon --stacktrace

      # ── Collect archives ────────────────────────────────────────────────────
      - name: Locate release archives
        id: archives
        run: |
          # archiveDir is $rootDir/../release  →  <repo-root>/release/
          ANDROID_ZIP=$(ls release/*-Android-v*.zip)
          IOS_ZIP=$(ls release/*-iOS-v*.zip)
          MULTI_ZIP=$(ls release/*-Multi-v*.zip)

          echo "android_zip=${ANDROID_ZIP}" >> "$GITHUB_OUTPUT"
          echo "ios_zip=${IOS_ZIP}"         >> "$GITHUB_OUTPUT"
          echo "multi_zip=${MULTI_ZIP}"     >> "$GITHUB_OUTPUT"

          echo "Found archives:"
          echo "  Android : ${ANDROID_ZIP}"
          echo "  iOS     : ${IOS_ZIP}"
          echo "  Multi   : ${MULTI_ZIP}"

      # ── Read plugin config ───────────────────────────────────────────────────
      - name: Read plugin config
        id: plugin_config
        run: |
          PLUGIN_NODE_NAME=$(grep '^pluginNodeName=' common/config/config.properties \
            | cut -d'=' -f2 | tr -d '[:space:]')
          echo "plugin_node_name=${PLUGIN_NODE_NAME}" >> "$GITHUB_OUTPUT"
          echo "Plugin node name: ${PLUGIN_NODE_NAME}"

          GODOT_VERSION=$(grep '^godotVersion=' common/config/config.properties \
            | cut -d'=' -f2 | tr -d '[:space:]')
          echo "godot_version=${GODOT_VERSION}" >> "$GITHUB_OUTPUT"
          echo "Godot version: ${GODOT_VERSION}"

      # ── Draft release ────────────────────────────────────────────────────────
      - name: Create draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ISSUE_LINES=$(gh issue list \
            --milestone "${{ github.event.milestone.title }}" \
            --state closed \
            --json number \
            --jq '.[].number | "          - #\(.)"')

          gh release create "$TAG_NAME" \
            --draft \
            --title "${{ steps.plugin_config.outputs.plugin_node_name }} Plugin $TAG_NAME" \
            --notes "## New in this release:

${ISSUE_LINES}

          <br />

          _This version..._

          -  _has been tested against and supports Godot version ${{ steps.plugin_config.outputs.godot_version }}._" \
            "${{ steps.archives.outputs.android_zip }}" \
            "${{ steps.archives.outputs.ios_zip }}" \
            "${{ steps.archives.outputs.multi_zip }}"
